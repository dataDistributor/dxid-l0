{
  "build": {
    "builder": "nixpacks",
    "buildCommand": "cargo build --release"
  },
  "deploy": {
    "startCommand": "./target/release/dxid-node --no-discovery",
    "healthcheckPath": "/health",
    "healthcheckTimeout": 300
  }
}
